# State

> Boot sector: the first read of every fresh session. Regenerated at the end of each work session — keep it one screen.

mfactory is in Build. Founding docs are complete (D-001…D-019). The enforcement pack (`enforcement/`), the scaffolder (`bin/mfactory-new`), and the loop driver (`bin/mfactory-loop`) exist; hooks are armed on this repo via `core.hooksPath`, and `.github/workflows/ci.yml` self-tests the pack — including the `enforcement/tests/` suites — on every push.

GitHub: `immaculatecross/mfactory` (public), default branch `master`, **branch protection armed** (required checks `self-check` + `review`, admins included). Identity (D-016): immaculatecross is the factory account; matmauro01 is the human's; the factory's token deliberately lacks `delete_repo` (`mfactory-smoke` still awaits Mattia's manual deletion).

The build loop is complete end to end: Foreman/builder playbooks (`verbs/`), work-order contract, adversarial reviewer (`verbs/review.md`) whose verdict is a PR comment opening with the exact line `VERDICT: approve|request-changes SHA=<head>` plus a `review` commit status, and `enforcement/ci/review-audit.sh` — the PR #4 fix, landed fresh via PRs #6/#7 — which validates only comment first lines against the current head, mutation-tested, on the Foreman's merge bar. `bin/mfactory-loop` allows one active driver per repo, re-invokes a fresh Foreman per cycle (D-003), obeys the strict final `NEXT:` sentinel, honors the `.mfactory/STOP` brake (D-014), caps runaway cycles, and takes any harness executable via `$MFACTORY_AGENT_CMD`. PRs #6 and #7 were themselves built, adversarially reviewed (one request-changes verdict caught a test that couldn't fail), repaired once, and merged through the full bar — the loop's PR leg is now live-proven on mfactory itself. Review severity is calibrated by D-018 (BLOCKING = demonstrated harm on a mandatory `Harm:` line; safe stops are advisory; re-review ratchet), and D-019 adds the adjudication ladder above a repeated rejection: fresh adjudicator sustains/overrules against the bar, overrides ride a `LOG.md` commit, escalation to Mattia is the last resort — the PR #9/#10 lesson encoded.

The shipped-versus-planned architecture, gate timing, and first Mora dogfood evidence are summarized in the offline, responsive `docs/architecture-now.html`.

Next ready work: a small real dogfood build (a tiny product through the full loop on GitHub) before any new machinery; then a generic `task` verb for one-off coding tasks on existing repos (the OpenClaw use case), F-008 (define verbs), F-007 (Owner). Deferred until a real run demands them: F-006 (task graph), F-009 (browser QA), F-012 (feedback pipeline). `gstack-main/` is unversioned reference — loot, don't import (D-001).
