# State

> Boot sector: the first read of every fresh session. Regenerated after every merge — keep it one screen.

__PRODUCT__ was scaffolded __DATE__. No code yet. Next: the Define phase (`ideate`, then `plan`).
